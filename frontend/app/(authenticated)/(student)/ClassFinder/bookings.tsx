import React from "react";
import { Text, View } from "react-native";
import { useLocalSearchParams } from "expo-router";
import BookingsPage from "@/src/components/ClassFinder/classrep/BookingsPage";

const Bookings = () => {
  return <BookingsPage />;
};

export default Bookings;
