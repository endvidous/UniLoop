import { Text, View } from "react-native";

const RequestAccountDetails = () => {
  return (
    <View>
      <Text>RequestAccountDetails</Text>
    </View>
  );
};

export default RequestAccountDetails;
